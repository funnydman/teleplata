language: python
python:
  - "3.6"

before_install:
 - sudo apt-get update
 - sudo apt-get install -y wkhtmltopdf

install:
  - pip install -r requirements.txt

script:
  - tox

after_success:
  - caveralls